name: Build WebView APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'tools/create-android-project.mjs'
      - 'tools/apply-android-branding.mjs'
      - '**.html'
      - '**.css'
      - '**.js'
      - 'assets/**'
      - 'data/**'
      - 'package.json'
      - '.github/workflows/build-webview-apk.yml'

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.9
      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Generate native WebView Android project
        run: node tools/create-android-project.mjs
      - name: Build debug APK
        run: gradle :app:assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: ydh-chronicle-webview-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
